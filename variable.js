//hello world

var a = 6;
console.log(a);

var outArray = ["Kapil", "J", "cat"];
outArray.push("Harish");//add element at the end of the array
outArray.pop();//remove element at the end of the array
outArray.shift(); //remove element at the start of the array
outArray.unshift("SUnil");//add element at the start of the array


var kap